<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Rappels du soir (flutter_local_notifications) : sans ces
         permissions et receivers, les notifications programmées ne sont
         JAMAIS délivrées sur Android — et ne survivent pas au redémarrage. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="Erea"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- AdMob. Le SDK lit cet identifiant au démarrage du processus et
             fait PLANTER l'app s'il est absent : il n'est donc pas
             commentable tant que google_mobile_ads est une dépendance.

             ⚠️ La valeur ci-dessous est l'app de démonstration de Google :
             elle sert au développement et ne rapporte rien. À remplacer
             par l'identifiant du compte AdMob d'Erea avant publication,
             en même temps que les blocs dans lib/core/pub.dart et le
             GADApplicationIdentifier de ios/Runner/Info.plist. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <!-- Classements Play Games — à activer le jour où le projet existe
             côté Google Play Console. Les deux meta-data ci-dessous sont
             exigées par games_services sur Android ; sans elles l'appel
             échoue en silence et le jeu reste jouable, classements masqués.

             Ne les décommente QU'UNE FOIS l'identifiant obtenu : une valeur
             absente ou vide fait planter l'app au démarrage de Play Games.
             L'identifiant est numérique, donc il passe obligatoirement par
             une ressource string, jamais en dur dans android:value.

             <meta-data
                 android:name="com.google.android.gms.games.APP_ID"
                 android:value="@string/play_games_app_id" />
             <meta-data
                 android:name="com.google.android.gms.version"
                 android:value="@integer/google_play_services_version" />
        -->

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
